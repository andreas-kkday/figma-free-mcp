{
  "name": "fig-local-context",
  "version": "0.1.0",
  "private": true,
  "description": "Local Figma .fig context extraction for coding agents",
  "type": "module",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "node --test tools/check-secrets.test.mjs tools/package-manifest.test.mjs tools/real-fig-smoke.test.mjs && pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "check:secrets": "node tools/check-secrets.mjs",
    "package:smoke": "pnpm build && node tools/package-smoke.mjs",
    "check": "pnpm build && pnpm typecheck && pnpm test && pnpm package:smoke && git diff --check && git diff --cached --check && pnpm check:secrets",
    "test:real-fig": "node tools/real-fig-smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
